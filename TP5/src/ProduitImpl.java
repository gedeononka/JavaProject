public class ProduitImpl extends Produit {
    // Add specific methods if needed
    public ProduitImpl() {
        super();  // Calls the parent constructor
    }

    public ProduitImpl(String ref, String libelle, double quantite, double prix) {
        super(ref, libelle, quantite, prix);  // Calls the parent constructor
    }
}
